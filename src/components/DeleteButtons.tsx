import React from 'react';
import Button from './Button';

const DeleteButtons = () => {
  return (
    <div>
        <Button text={'Delete done tasks'}></Button>
        <Button text={'Delete all tasks'}></Button>
    </div>
  )
}

export default DeleteButtons;
